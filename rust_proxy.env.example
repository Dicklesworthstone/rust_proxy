# rust_proxy environment file template
# Copy this to /etc/rust_proxy.env and fill in your credentials
#
# These environment variables are used when proxy auth is configured
# with --username-env and --password-env options

# Proxy authentication credentials
PROXY_USER=your_username_here
PROXY_PASS=your_password_here

# Additional proxy-specific credentials (if using multiple proxies)
# PROXY_USER_MESH=mesh_username
# PROXY_PASS_MESH=mesh_password
